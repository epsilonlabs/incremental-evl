context Tree {

	constraint hasLabel {
		check : self.children.forAll(t : Tree | 
			not t.labelContainer.label.isUndefined()
		)
	}
}

context LabelContainer {
		constraint hasLabel {
		check : not self.label.isUndefined()
	}
}